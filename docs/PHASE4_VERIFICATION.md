# Проверка фазы 4

Дата: 23.09.2026. Фаза: предложение, подтверждение и сохранение корзины.

## Автоматические проверки

- `npm test`: 22 проверки, все успешны (14 прежних + 8 для корзины).
- `npm run lint`, `npm run typecheck`, `npm run build`: успешны.
- Проверены неизвестные цены/остатки, дробные и отрицательные количества,
  исключение технических складов, распределение по городам и предотвращение
  повторного учёта одного остатка в нескольких строках.
- Проверены подпись данных, неверный ключ, подмена и использование токена
  другого назначения, обнаружение изменения цены и распределения складов.
- Частичный комплект отличается от полного, конфликт номинала блокирует
  предложение, замена требует сравнения и предупреждения.

## Живой API EKT

`node scripts/verify-cart.mjs`: PASS. Скрипт использует отдельный cookie jar,
проверяет 11 сценариев и очищает только свою тестовую корзину.

027024: 10 ед., 5 в Алматы, остальное с других городских складов. На момент
проверки цена 29 810 ₸, сумма 298 100 ₸. Значения меняются вместе с API.

Проверены отсутствие изменения при quote, обязательное подтверждение и
ознакомление с условиями, отклонение изменённого токена и чужой сессии,
сохранение корзины, идемпотентность повтора, блокировка 027228 с конфликтом
160/250 А, подтверждение очистки и отклонение устаревшего предложения.

## Браузер

- Реальный чат: запрос 10 штук 027024 → вариант 5 местных + 5 других →
  подтверждение состава → ссылка на отдельную корзину.
- После перезагрузки сохраняются товар, количество и сумма.
- На телефоне количество изменено с 10 на 3, показано новое предложение,
  подтверждено и проверено после перезагрузки.
- Desktop 1262px и mobile 390px: страница, подтверждение, ссылки и кнопки работают,
  горизонтального переполнения нет; browser errors пусты.
- Субагент отдельно проверил основную страницу на ширинах 1262, 390 и 320px,
  поиск и смену города. Его изменения ограничены внешним видом и обёрткой страницы.

## Границы

Это проверенные сценарии, не статистическая оценка качества. Корзина живёт
в этом браузере; нет публичной ссылки на состав, резервирования или заказа в EKT.
Нет атомарной транзакции между вкладками. Доставка требует согласования;
кандидаты на замену не объявляются сертифицированно взаимозаменяемыми.
Фаза 5 (фото, файлы, экспорт) не начата.

## Опубликованная версия

Коммит реализации: `68436a1`. Production deployment:
`dpl_6qx6JzYPnBXqLQuGHBC7PfX5C6yF`, статус READY.

На https://komplekt-ai-zhigitter.vercel.app повторены все 11 сценариев
`scripts/verify-cart.mjs`: PASS. В браузере проверены главная, новая панель
комплекта и переход на `/cart`; ошибок браузера нет. Тесты используют
отдельную сессию и не изменяют корзину пользователя.
